# 都道府県・市区町村選択ツール v61.0 (GitHub GIS連携版) - 必要ライブラリ
# Prefecture City Selector v61.0 (GitHub GIS Integration) - Required Dependencies

# Webアプリケーションフレームワーク
streamlit>=1.28.0

# データ処理
pandas>=1.5.0

# Excelファイル読み込み
openpyxl>=3.0.0

# HTTP通信
requests>=2.28.0

# GIS データ処理（重要）
geopandas>=0.13.0
fiona>=1.8.0
pyproj>=3.4.0
shapely>=2.0.0

# XML処理（KMLファイル用）
lxml>=4.9.0

# HTML解析（GitHub代替取得用）
beautifulsoup4>=4.11.0

# ZIP処理（標準ライブラリのzipfileを使用するが、より高度な処理用）
# zipfile は標準ライブラリなので記載不要

# 正規表現処理（標準ライブラリのreを使用）
# re は標準ライブラリなので記載不要

# 一時ファイル処理（標準ライブラリのtempfileを使用）
# tempfile は標準ライブラリなので記載不要

# 地理空間データベースサポート（オプション）
# gdal>=3.5.0

# 開発・デバッグ用（オプション）
# pytest>=7.0.0
# pytest-cov>=4.0.0

# 注意:
# - GitHub連携とGIS機能を利用するためgeopandas、beautifulsoup4等が必要です
# - 対応ファイル形式: ZIP, Shapefile(.shp), KML および関連ファイル(.shx, .prj, .dbf, .cpg)
# - Python 3.8+ を推奨します
# - GDALライブラリが必要な場合があります（GeoPandasの依存関係）

# インストール方法:
# pip install -r requirements.txt

# GIS環境での推奨インストール:
# conda install -c conda-forge geopandas
# pip install streamlit openpyxl requests beautifulsoup4

# 実行方法:
# streamlit run prefecture_city_selector_streamlit.py

# 仮想環境での使用を推奨:
# python -m venv venv
# source venv/bin/activate  # Windows: venv\Scripts\activate
# pip install -r requirements.txt
# streamlit run prefecture_city_selector_streamlit.py

# Streamlit Cloud デプロイメント用:
# - このファイルをリポジトリのルートに配置
# - 自動認識されます
# - GIS機能のため初回起動時に時間がかかる場合があります

# Docker環境での使用:
# - base imageはpython:3.9-slim以上を推奨
# - apt-get install -y gdal-bin libgdal-dev が必要な場合があります

# 主要機能別依存関係:
# ✅ 基本機能: streamlit, pandas, openpyxl, requests
# 🗺️ GIS機能: geopandas, fiona, pyproj, shapely, lxml
# 🌐 GitHub連携: requests, beautifulsoup4
# 📊 データ処理: pandas (標準)
# 🔍 ファイル検索: 標準ライブラリ (os, glob, re, zipfile, tempfile)

# パフォーマンス最適化（オプション）:
# numba>=0.56.0  # 数値計算高速化
# rtree>=1.0.0   # 空間インデックス高速化